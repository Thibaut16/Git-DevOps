pipeline {
    agent any
    stages {
        stage('DEBUG') {
            steps {
                echo 'Jenkinsfile wird ausgeführt ✅'
                sh 'whoami'
                sh 'pwd'
            }
        }
    }
}

